// Função que calcula o desconto. (expressão de função)

function calcularDescontos (valor, desconto) {
    let descontoAplicado = desconto/100
    descontoAplicado = descontoAplicado.toFixed(2)
    
    const valorFinal = valor*(1 - descontoAplicado)
    return valorFinal
}

const descontoFinal = function(calcularDescontos) {
    const valorFinal = valor*(1 - descontoFinal)
let valorTotal = valor + desconto
return valorTotal
}

console.log("Total : " + calcularDescontos(100, 10))