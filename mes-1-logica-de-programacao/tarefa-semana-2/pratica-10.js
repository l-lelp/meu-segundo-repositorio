// Qual é o maior de 3 números?

function maiorNumero(x , y, z) {
    const maiorNumero = x
    const menorNumero = y
    const maiorNumeroMaior = z

 console.log(Math.max(x,y,z))
}

maiorNumero(-30, 10, -100)